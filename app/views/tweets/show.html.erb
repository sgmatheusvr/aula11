<div class="container">


        <div class="card" style="width: 20rem;">
          <img class="card-img-top" src="..." alt="Card image cap">
          <div class="card-block">
            <h4 class="card-title"><%= @tweet.user.email %></h4>
            <p class="card-text"><%= @tweet.conteudo %></p>
              <p class="card-text">(<%= @tweet.created_at.strftime("%d/%m/%Y - %H:%M") %>)</p>
          <%= link_to 'Back', tweets_path, class: "btn btn-primary btn-sm" %>
              
             <% if @tweet.user == current_user %>
              
             | <%= link_to 'Editar', edit_tweet_path(@tweet), class: "btn btn-info btn-sm" %>
             | <%= link_to 'Apagar', @tweet, method: :delete, class: "btn btn-danger btn-sm", data: { confirm: 'Você tem certeza?'} %>
              
              <% end %>
              
          </div>

            
    </div>  
            
            





        
</div>